# =============================================================
# Runtime dependencies (encoding + evaluation only).
# These are enough to load a checkpoint and reproduce the
# retrieval/effectiveness numbers from the paper.
# =============================================================
torch>=2.6
transformers>=4.54,<4.55
accelerate>=1.0
peft>=0.13
datasets>=4.0
safetensors>=0.4
einops>=0.8
nltk>=3.9
numpy
pandas
pyarrow
pytrec-eval-terrier>=0.5
tqdm
wandb

# =============================================================
# Training-only (optional — install with requirements-train.txt
# if you want to retrain from scratch). Pulls in DeepSpeed; the
# Tevatron-derived training infrastructure is vendored under
# src/training/.
# =============================================================